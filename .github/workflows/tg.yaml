name: demo
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - env:
        TEST: ${{ secrets.TEST }}
      - run: |
          echo "Hello!"
          echo `date`
          echo $TEST
          echo "Bye!"
